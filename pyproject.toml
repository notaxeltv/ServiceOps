[project]
name = "serviceops"
version = "0.1.0"
description = "ServiceOps — piattaforma operativa per la gestione dei servizi"
readme = "README.md"
requires-python = ">=3.10"

[project.optional-dependencies]
graphify = ["graphifyy>=0.9.48"]

[tool.uv]
dev-dependencies = []
